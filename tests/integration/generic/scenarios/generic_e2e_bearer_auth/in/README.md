# Generic fetcher bearer auth integration test

This test case checks that Hermeto can fetch a generic artifact using bearer token authentication.
The artifact is fetched from a local Nexus proxy with a bearer token.
