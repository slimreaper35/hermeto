# Generic fetcher wrong credentials integration test

This test case checks that Hermeto correctly handles a generic artifact fetch when wrong basic auth
credentials are provided. The request is expected to fail with an authentication error.
