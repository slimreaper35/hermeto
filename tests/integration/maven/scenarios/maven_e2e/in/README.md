# Maven parent POMs test scenario

This project contains a Spring Boot Web project and is meant to test the inclusion of parent POMs in
the lockfile.

## How to run it locally

```bash
mvn clean package
java -jar target/demo-0.0.1-SNAPSHOT.jar
```
